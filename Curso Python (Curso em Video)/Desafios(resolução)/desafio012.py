print("Promoção")

preco_original = float(input("Digite o valor do produto (sem desconto): "))

preco_desconto = preco_original * 0.95

print(f"O valor de R${preco_original} com desconto de 5% fica R${preco_desconto}")